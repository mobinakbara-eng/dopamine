"use strict";
function kcol(title,sub,ic,items,cls){return`<section class="kiosk-column ${cls}"><div class="kiosk-column-head"><div class="metric-icon">${ic}</div><div><h2>${title}</h2><p>${sub}</p></div><div class="count-box">${items.length}</div></div>${items.length?`<div class="kiosk-list">${items.map(kperson).join("")}</div>`:`<div class="kiosk-empty"><div>${ic}<strong>${title==="Pause"?"Aktuell niemand in Pause":"Keine Mitarbeiter"}</strong><p>Namen können hierher gezogen werden.</p></div></div>`}<div class="column-foot">${I.people}&nbsp;&nbsp;${items.length} Mitarbeiter</div></section>`}function kperson(e){return`<button class="kiosk-person ${S.selected===e.id?"selected":""}" data-a="select-person" data-id="${e.id}"><div class="avatar">${esc(e.initials||initials(e.name))}</div><span><strong>${esc(e.name)}</strong><small>${esc(e.role)}</small></span><b class="drag-dots">⠿</b></button>`}function kioskActions(){if(!S.selected)return"";const en=live(S.selected);return`<div class="kiosk-actionbar">${!en?`<button class="btn" data-a="transition" data-target="in">Einstempeln</button>`:en.status==="live"?`<button class="btn outline" data-a="transition" data-target="pause">Pause</button><button class="btn" data-a="transition" data-target="out">Feierabend</button>`:`<button class="btn outline" data-a="transition" data-target="in">Pause beenden</button><button class="btn" data-a="transition" data-target="out">Feierabend</button>`}<button class="btn light" data-a="clear-person">Abbrechen</button></div>`}
