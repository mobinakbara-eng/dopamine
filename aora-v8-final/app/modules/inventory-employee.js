"use strict";
let inventoryScannerStream=null,inventoryScannerTimer=null,inventoryScannerBusy=false;
async function ensureEmployeeInventoryAvailability(employee){if(!S.session||S.accessRole!=="employee")return;const locationId=employee?.locationId||S.session?.locationId||S.locationId;if(!locationId)return;const key=`${S.session.token}:${locationId}`;if(S.inventoryAvailabilityCache[key]!==undefined)return;S.inventoryAvailabilityCache[key]=null;try{S.inventoryAvailabilityCache[key]=await invRequest("availability",{locationId})}catch{S.inventoryAvailabilityCache[key]={enabled:false}}if(S.employeeView==="more")renderEmployee()}
const _employeeView=employeeView;employeeView=function(employee,view){if(view==="inventory-scan"){queueMicrotask(()=>startInventoryScanner(employee));return inventoryScanView(employee)}if(view!=="inventory-scan")stopInventoryScanner();let html=_employeeView(employee,view);if(view==="more"){const locationId=employee?.locationId||S.session?.locationId||S.locationId,a=S.inventoryAvailabilityCache[`${S.session?.token}:${locationId}`];if(a?.enabled&&a?.permissions?.consume)html=html.replace('<button class="btn outline" style="width:100%;margin-top:14px" data-a="logout">',`<button class="inventory-more-card" type="button" data-inv="employee-scan"><span><strong>QR scannen</strong><small>Verbrauch direkt aus dem Bestand buchen</small></span>${I.arrow}</button><button class="btn outline" style="width:100%;margin-top:14px" data-a="logout">`)}return html};
const _renderEmployee=renderEmployee;renderEmployee=function(){const employee=emp(S.session?.subjectId)||S.state?.employees?.[0];_renderEmployee();queueMicrotask(()=>ensureEmployeeInventoryAvailability(employee))};
function inventoryScanView(employee){return`<section class="inventory-scan-page"><div class="inventory-scan-title"><div class="caps muted">Bestand · ${esc(loc(employee.locationId)?.name||"")}</div><h1>QR scannen</h1><p>QR-Code auf einer Verpackung scannen.</p></div><div class="inventory-scanner"><video id="inventory-camera" playsinline muted></video><div class="inventory-scan-frame"></div></div><div id="inventory-scan-result" class="inventory-scan-status">Kamera wird gestartet …</div><form class="inventory-shortcode" id="inventory-shortcode"><input class="input" name="shortCode" placeholder="Kurzcode eingeben" autocomplete="off"><button class="btn" type="submit">Buchen</button></form></section>`}
async function startInventoryScanner(employee){if(S.employeeView!=="inventory-scan")return;stopInventoryScanner();const video=document.getElementById("inventory-camera"),result=document.getElementById("inventory-scan-result");if(!video)return;try{inventoryScannerStream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:"environment"}},audio:false});video.srcObject=inventoryScannerStream;await video.play();result.textContent="QR-Code in den Rahmen halten.";const canvas=document.createElement("canvas"),ctx2=canvas.getContext("2d",{willReadFrequently:true});let detector=null;if("BarcodeDetector" in window){try{detector=new BarcodeDetector({formats:["qr_code"]})}catch{}}const tick=async()=>{if(S.employeeView!=="inventory-scan"||!inventoryScannerStream)return;if(!inventoryScannerBusy&&video.readyState>=2){try{let code="";if(detector){const found=await detector.detect(video);code=found?.[0]?.rawValue||""}else if(window.jsQR){canvas.width=video.videoWidth;canvas.height=video.videoHeight;ctx2.drawImage(video,0,0);const image=ctx2.getImageData(0,0,canvas.width,canvas.height);code=window.jsQR(image.data,image.width,image.height,{inversionAttempts:"dontInvert"})?.data||""}if(/^A1\.k1\./.test(code))await processInventoryQr(code,employee)}catch{}}inventoryScannerTimer=setTimeout(tick,250)};tick()}catch{result.innerHTML='<strong>Kamera nicht verfügbar.</strong><br>Bitte Kamerazugriff erlauben oder den Kurzcode eingeben.'}document.getElementById("inventory-shortcode")?.addEventListener("submit",async e=>{e.preventDefault();const f=e.currentTarget,value=String(new FormData(f).get("shortCode")||"").trim();if(!value)return;try{inventoryScannerBusy=true;const data=await invRequest("issueQrShortCode",{locationId:employee.locationId,shortCode:value,idempotencyKey:crypto.randomUUID()});showInventoryScanSuccess(data);f.reset()}catch(err){toast(err.message,"error")}finally{setTimeout(()=>inventoryScannerBusy=false,900)}})}
async function processInventoryQr(token,employee){inventoryScannerBusy=true;try{const data=await invRequest("issueQrUnit",{locationId:employee.locationId,token,idempotencyKey:crypto.randomUUID()});showInventoryScanSuccess(data);navigator.vibrate?.(80)}catch(err){const n=document.getElementById("inventory-scan-result");if(n)n.innerHTML=`<strong>${esc(err.message)}</strong>`}finally{setTimeout(()=>inventoryScannerBusy=false,1100)}}
function showInventoryScanSuccess(data){const n=document.getElementById("inventory-scan-result");if(!n)return;n.className="inventory-scan-success";n.innerHTML=`<strong>✓ Erfasst</strong><span>Bestand jetzt: ${Number(data.onHand??0)}</span>`;setTimeout(()=>{if(n.isConnected){n.className="inventory-scan-status";n.textContent="Weiter scannen …"}},1100)}
function stopInventoryScanner(){if(inventoryScannerTimer)clearTimeout(inventoryScannerTimer);inventoryScannerTimer=null;inventoryScannerStream?.getTracks?.().forEach(t=>t.stop());inventoryScannerStream=null;inventoryScannerBusy=false}
app.addEventListener("click",e=>{const b=e.target.closest("[data-inv]");if(b?.dataset.inv==="employee-scan"){S.employeeView="inventory-scan";renderEmployee()}});
