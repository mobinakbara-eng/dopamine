"use strict";
const _inventoryOrders=inventoryOrders;inventoryOrders=function(d){return _inventoryOrders(d).replace('<button class="btn outline" data-inv="suppliers">Lieferanten</button>','<button class="btn outline" data-inv="ordering-profile">Absenderdaten</button><button class="btn outline" data-inv="suppliers">Lieferanten</button>')};
async function orderingProfileModal(){const d=modal(`${modalHeader("Bestellen","Absenderdaten")}<div id="ordering-profile"><div class="inventory-empty">Wird geladen …</div></div>`);try{const r=await invRequest("getOrderingProfile",{locationId:S.locationId}),p=r.profile||{};d.querySelector("#ordering-profile").innerHTML=`<form class="form-grid"><div class="field"><label>Café / Standort</label><input class="input" name="cafeName" value="${esc(p.cafe_name||"")}" required></div><div class="field"><label>Firma</label><input class="input" name="legalName" value="${esc(p.legal_name||"")}"></div><div class="field full"><label>Lieferadresse</label><input class="input" name="address" value="${esc(p.address||"")}"></div><div class="field"><label>PLZ</label><input class="input" name="postalCode" value="${esc(p.postal_code||"")}"></div><div class="field"><label>Stadt</label><input class="input" name="city" value="${esc(p.city||"")}"></div><div class="field"><label>Telefon</label><input class="input" name="phone" value="${esc(p.phone||"")}"></div><div class="field"><label>Bestell-E-Mail</label><input class="input" type="email" name="orderingEmail" value="${esc(p.ordering_email||"")}"></div><div class="field"><label>Reply-to E-Mail</label><input class="input" type="email" name="replyToEmail" value="${esc(p.reply_to_email||"")}"></div><div class="field"><label>WhatsApp des Cafés</label><input class="input" name="whatsappNumber" value="${esc(p.whatsapp_number||"")}"></div><div class="field"><label>Kundennummer</label><input class="input" name="customerNumber" value="${esc(p.customer_number||"")}"></div><div class="field"><label>USt-ID</label><input class="input" name="vatId" value="${esc(p.vat_id||"")}"></div><div class="field full"><label>Signatur</label><textarea class="input" name="signature" rows="4">${esc(p.signature||"")}</textarea></div><div class="field full actions"><button type="button" class="btn outline" data-a="close">Abbrechen</button><button class="btn" type="submit">Speichern</button></div></form>`;d.querySelector("form").addEventListener("submit",async e=>{e.preventDefault();const f=new FormData(e.currentTarget),body={locationId:S.locationId};for(const k of["cafeName","legalName","address","postalCode","city","phone","orderingEmail","replyToEmail","whatsappNumber","customerNumber","vatId","signature"])body[k]=f.get(k)||"";try{await invRequest("saveOrderingProfile",body);d.remove();toast("Absenderdaten gespeichert.")}catch(err){toast(err.message,"error")}})}catch(e){d.querySelector("#ordering-profile").innerHTML=`<div class="inventory-empty">${esc(e.message)}</div>`}}
app.addEventListener("click",e=>{const b=e.target.closest("[data-inv]");if(b?.dataset.inv==="ordering-profile")orderingProfileModal()});
