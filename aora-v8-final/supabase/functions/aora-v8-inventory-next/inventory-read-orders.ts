import {db,dbFail,requirePermission,type InventoryContext} from "./lib.ts";
export async function listTransfers(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"view",requestId);const{data,error}=await db.from("inventory_transfers").select("id,source_location_id,destination_location_id,status,note,version,created_at,dispatched_at,received_at").eq("organization_id",ctx.organizationId).or(`source_location_id.eq.${locationId},destination_location_id.eq.${locationId}`).order("created_at",{ascending:false}).limit(150);if(error)dbFail(error,"transfers",requestId);return{locationId,transfers:data||[]}}
export async function listPurchaseOrders(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"procurement",requestId);const{data:orders,error}=await db.from("inventory_purchase_orders").select("id,supplier_id,status,expected_on,note,version,order_number,delivery_channel,provider_message_id,provider_status,last_error,created_at,submitted_at,sent_at,received_at").eq("organization_id",ctx.organizationId).eq("location_id",locationId).order("created_at",{ascending:false}).limit(150);if(error)dbFail(error,"purchase_orders",requestId);const orderIds=(orders||[]).map((r:any)=>r.id);if(!orderIds.length)return{locationId,orders:[]};const supplierIds=[...new Set((orders||[]).map((r:any)=>r.supplier_id))];const[{data:lines,error:le},{data:suppliers,error:se}]=await Promise.all([db.from("inventory_purchase_order_lines").select("purchase_order_id,item_id,ordered_quantity,received_quantity,unit_cost,supplier_item_id,pack_unit_id,ordered_pack_quantity,supplier_sku,supplier_item_name").eq("organization_id",ctx.organizationId).in("purchase_order_id",orderIds),db.from("inventory_suppliers").select("id,name,contact").eq("organization_id",ctx.organizationId).in("id",supplierIds)]);if(le||se)dbFail(le||se,"purchase_order_context",requestId);const itemIds=[...new Set((lines||[]).map((r:any)=>r.item_id))],packIds=[...new Set((lines||[]).map((r:any)=>r.pack_unit_id).filter(Boolean))];const[{data:items,error:ie},{data:packs,error:pe}]=await Promise.all([itemIds.length?db.from("inventory_items").select("id,name,sku,base_uom").eq("organization_id",ctx.organizationId).in("id",itemIds):Promise.resolve({data:[],error:null}),packIds.length?db.from("inventory_pack_units").select("id,label,code,base_quantity").eq("organization_id",ctx.organizationId).in("id",packIds):Promise.resolve({data:[],error:null})]);if(ie||pe)dbFail(ie||pe,"purchase_order_items",requestId);const sm=new Map((suppliers||[]).map((r:any)=>[String(r.id),r])),im=new Map((items||[]).map((r:any)=>[String(r.id),r])),pm=new Map((packs||[]).map((r:any)=>[String(r.id),r]));return{locationId,orders:(orders||[]).map((o:any)=>({...o,supplier:sm.get(String(o.supplier_id))||null,lines:(lines||[]).filter((l:any)=>String(l.purchase_order_id)===String(o.id)).map((l:any)=>({...l,orderedQuantity:Number(l.ordered_quantity),receivedQuantity:Number(l.received_quantity),orderedPackQuantity:l.ordered_pack_quantity==null?null:Number(l.ordered_pack_quantity),unitCost:l.unit_cost==null?null:Number(l.unit_cost),item:im.get(String(l.item_id))||null,pack:pm.get(String(l.pack_unit_id))||null}))}))}}
export async function listPackUnits(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"view",requestId);const{data,error}=await db.from("inventory_pack_units").select("id,item_id,code,label,base_quantity,is_stock_unit,is_order_unit,active").eq("organization_id",ctx.organizationId).eq("item_id",String(body.itemId||"")).eq("active",true).order("base_quantity");if(error)dbFail(error,"pack_units",requestId);return{packUnits:(data||[]).map((r:any)=>({...r,baseQuantity:Number(r.base_quantity)}))}}
export async function listPrintJobs(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"receipt",requestId);const{data,error}=await db.from("inventory_label_print_jobs").select("id,receipt_id,purchase_order_id,item_id,pack_unit_id,label_count,status,generation,prepared_at,printed_at,created_at").eq("organization_id",ctx.organizationId).eq("location_id",locationId).in("status",["pending","prepared"]).order("created_at",{ascending:false}).limit(150);if(error)dbFail(error,"print_jobs",requestId);const ids=[...new Set((data||[]).map((r:any)=>r.item_id))];const{data:items,error:ie}=ids.length?await db.from("inventory_items").select("id,name,sku,base_uom").eq("organization_id",ctx.organizationId).in("id",ids):{data:[],error:null};if(ie)dbFail(ie,"print_job_items",requestId);const im=new Map((items||[]).map((r:any)=>[String(r.id),r]));return{locationId,jobs:(data||[]).map((r:any)=>({...r,labelCount:Number(r.label_count),generation:Number(r.generation),item:im.get(String(r.item_id))||null}))}}
export async function listEmployeeAccess(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"view",requestId);const[{data:employees,error:ee},{data:additional,error:ae},{data:grants,error:ge}]=await Promise.all([db.from("employees").select("id,name,email,role,role_title,location_id,primary_location_id").eq("organization_id",ctx.organizationId).eq("active",true).is("deleted_at",null),db.from("employee_location_access").select("employee_id").eq("organization_id",ctx.organizationId).eq("location_id",locationId),db.from("inventory_permission_grants").select("subject_id").eq("organization_id",ctx.organizationId).eq("subject_type","employee").eq("location_id",locationId).eq("permission","consume")]);if(ee||ae||ge)dbFail(ee||ae||ge,"employee_access",requestId);const extra=new Set((additional||[]).map((r:any)=>String(r.employee_id))),granted=new Set((grants||[]).map((r:any)=>String(r.subject_id)));return{locationId,employees:(employees||[]).filter((r:any)=>[r.primary_location_id,r.location_id].filter(Boolean).map(String).includes(locationId)||extra.has(String(r.id))).map((r:any)=>({id:String(r.id),name:String(r.name||"Mitarbeiter"),email:String(r.email||""),roleTitle:String(r.role_title||r.role||"Mitarbeiter"),scanEnabled:granted.has(String(r.id))})).sort((a:any,b:any)=>a.name.localeCompare(b.name,"de"))}}
export async function listReplenishment(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"procurement",requestId);const{data,error}=await db.from("inventory_replenishment_state").select("item_id,below_threshold,episode_id,opened_at,suggested_base_quantity,updated_at").eq("organization_id",ctx.organizationId).eq("location_id",locationId).eq("below_threshold",true).order("opened_at");if(error)dbFail(error,"replenishment",requestId);return{suggestions:(data||[]).map((r:any)=>({...r,suggestedQuantity:Number(r.suggested_base_quantity)}))}}
