import QRCode from "npm:qrcode@1.5.4";
import {db,dbFail,fail,now,asUuid,idem,qrToken,shortCode,sha256Hex,requirePermission,requireFeature,type InventoryContext} from "./lib.ts";
const PRINT_PROFILES:any={zebra_50x30_203:{profileKey:"zebra_50x30_203",labelWidthMm:50,labelHeightMm:30,qrSizeMm:22,dpi:203,mediaType:"direct_thermal_gap",connectionMode:"system_dialog",printerModel:"Zebra ZD421d"},brother_62x29_300:{profileKey:"brother_62x29_300",labelWidthMm:62,labelHeightMm:29,qrSizeMm:21,dpi:300,mediaType:"direct_thermal_gap",connectionMode:"airprint",printerModel:"Brother QL-820NWBc"},generic_pdf_50x30:{profileKey:"generic_pdf_50x30",labelWidthMm:50,labelHeightMm:30,qrSizeMm:22,dpi:300,mediaType:"direct_thermal_gap",connectionMode:"system_dialog",printerModel:"PDF / Systemdrucker"}};
export async function receiveQrUnits(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"receipt",requestId);await Promise.all([requireFeature(ctx,"inventory_qr",locationId,requestId),requireFeature(ctx,"inventory_printing",locationId,requestId)]);const count=Math.trunc(Number(body.count));if(!Number.isSafeInteger(count)||count<1||count>100)fail(400,"unit_count_invalid","Bitte 1 bis 100 Einheiten angeben.");const{data,error}=await db.rpc("aora_inventory_receive_pending_labels",{p_organization_id:ctx.organizationId,p_location_id:locationId,p_item_id:asUuid(body.itemId,"item"),p_pack_unit_id:asUuid(body.packUnitId,"pack_unit"),p_count:count,p_actor_id:ctx.subjectId,p_actor_role:ctx.accessRole,p_idempotency_key:idem(body.idempotencyKey),p_purchase_order_id:body.purchaseOrderId?asUuid(body.purchaseOrderId,"purchase_order"):null});if(error){const m=String(error.message||"");if(m.includes("quantity_exceeded"))fail(409,"purchase_order_quantity_exceeded","Die eingegangene Menge ist höher als die noch offene Bestellmenge.");if(m.includes("not_receivable"))fail(409,"purchase_order_not_receivable","Die Bestellung ist nicht mehr für einen Wareneingang offen.");dbFail(error,"receive_qr_units",requestId)}return data}
export async function preparePrintJob(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"receipt",requestId);await Promise.all([requireFeature(ctx,"inventory_qr",locationId,requestId),requireFeature(ctx,"inventory_printing",locationId,requestId)]);const printJobId=asUuid(body.printJobId,"print_job");const{data:job,error:je}=await db.from("inventory_label_print_jobs").select("label_count,item_id,status").eq("organization_id",ctx.organizationId).eq("location_id",locationId).eq("id",printJobId).maybeSingle();if(je)dbFail(je,"load_print_job",requestId);if(!job)fail(404,"print_job_not_found","Der Druckauftrag wurde nicht gefunden.");if(job.status==="printed")fail(409,"print_job_already_printed","Dieser Druckauftrag wurde bereits bestätigt.");const count=Number(job.label_count),labels:any[]=[],units:any[]=[];for(let i=0;i<count;i++){const token=qrToken(),code=shortCode();labels.push({token,shortCode:code,sequence:i+1,total:count});units.push({token_hash:await sha256Hex(token),short_code:code})}const{data,error}=await db.rpc("aora_inventory_prepare_print_job",{p_organization_id:ctx.organizationId,p_location_id:locationId,p_print_job_id:printJobId,p_units:units,p_actor_id:ctx.subjectId});if(error)dbFail(error,"prepare_print_job",requestId);const printable=await Promise.all(labels.map(async l=>({...l,svg:await QRCode.toString(l.token,{type:"svg",errorCorrectionLevel:"M",margin:4,width:220})})));return{...data,itemId:job.item_id,labels:printable,replacedPrevious:job.status==="prepared"}}
export async function confirmPrintJob(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"receipt",requestId);const{data,error}=await db.rpc("aora_inventory_confirm_print_job",{p_organization_id:ctx.organizationId,p_location_id:locationId,p_print_job_id:asUuid(body.printJobId,"print_job"),p_actor_id:ctx.subjectId});if(error)dbFail(error,"confirm_print_job",requestId);return data}
export async function issueQrUnit(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"consume",requestId);if(ctx.accessRole==="employee"){await requireFeature(ctx,"inventory_qr",locationId,requestId);await requireFeature(ctx,"inventory_employee_scan",locationId,requestId)}const token=String(body.token||"").trim();if(!/^A1\.k1\.[A-Za-z0-9_-]{24,80}$/.test(token))fail(400,"qr_invalid","Der QR-Code ist ungültig.");const{data,error}=await db.rpc("aora_inventory_issue_qr_unit",{p_organization_id:ctx.organizationId,p_location_id:locationId,p_token_hash_hex:await sha256Hex(token),p_actor_id:ctx.subjectId,p_actor_role:ctx.accessRole,p_idempotency_key:idem(body.idempotencyKey)});if(error){const m=String(error.message||"");if(m.includes("already_used"))fail(409,"qr_already_used","Diese Einheit wurde bereits verwendet oder gehört zu einem anderen Standort.");if(m.includes("not_found"))fail(404,"qr_not_found","QR-Code wurde nicht gefunden.");dbFail(error,"issue_qr",requestId)}return data}
export async function setEmployeeAccess(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"view",requestId);if(ctx.accessRole==="employee")fail(403,"manager_required","Nur Manager oder Inhaber dürfen Scan-Freigaben verwalten.");const{data,error}=await db.rpc("aora_inventory_set_employee_scan_access",{p_organization_id:ctx.organizationId,p_location_id:locationId,p_employee_id:String(body.employeeId||""),p_enabled:Boolean(body.enabled),p_actor_id:ctx.subjectId,p_actor_role:ctx.accessRole});if(error){const m=String(error.message||"");if(m.includes("employee_not_found"))fail(404,"employee_not_found","Mitarbeiter wurde nicht gefunden.");if(m.includes("location_forbidden"))fail(403,"employee_location_forbidden","Der Mitarbeiter gehört nicht zu diesem Standort.");dbFail(error,"set_employee_access",requestId)}return data}
export async function getPrintProfile(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"receipt",requestId);const{data,error}=await db.from("inventory_print_profiles").select("profile_key,connection_mode,printer_model,label_width_mm,label_height_mm,qr_size_mm,dpi,media_type,updated_at").eq("organization_id",ctx.organizationId).eq("location_id",locationId).maybeSingle();if(error)dbFail(error,"print_profile",requestId);if(!data)return{...PRINT_PROFILES.brother_62x29_300,locationId,persisted:false};return{locationId,profileKey:data.profile_key,connectionMode:data.connection_mode,printerModel:data.printer_model,labelWidthMm:Number(data.label_width_mm),labelHeightMm:Number(data.label_height_mm),qrSizeMm:Number(data.qr_size_mm),dpi:Number(data.dpi),mediaType:data.media_type,updatedAt:data.updated_at,persisted:true}}
export async function savePrintProfile(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"receipt",requestId);const p=PRINT_PROFILES[String(body.profileKey||"")];if(!p)fail(400,"print_profile_invalid","Bitte ein unterstütztes Druckprofil auswählen.");const row={organization_id:ctx.organizationId,location_id:locationId,profile_key:p.profileKey,connection_mode:p.connectionMode,printer_model:p.printerModel,label_width_mm:p.labelWidthMm,label_height_mm:p.labelHeightMm,qr_size_mm:p.qrSizeMm,dpi:p.dpi,media_type:p.mediaType,updated_by:ctx.subjectId,updated_at:now()};const{error}=await db.from("inventory_print_profiles").upsert(row,{onConflict:"organization_id,location_id"});if(error)dbFail(error,"save_print_profile",requestId);return{...p,locationId,persisted:true,updatedAt:row.updated_at}}
export async function printTestLabel(ctx:InventoryContext,body:any,requestId:string){const p=await getPrintProfile(ctx,body,requestId),value=`AORA.PRINT.TEST.${p.profileKey}`;return{profile:p,labels:[{test:true,shortCode:"TEST-0001",sequence:1,total:1,svg:await QRCode.toString(value,{type:"svg",errorCorrectionLevel:"M",margin:4,width:220})}]}}
