import {db,dbFail,fail,asUuid,idem,requirePermission,requireFeature,type InventoryContext} from "./lib.ts";
export async function startInventoryCount(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"adjust",requestId);await requireFeature(ctx,"inventory_counting",locationId,requestId);const{data,error}=await db.rpc("aora_inventory_start_count",{p_organization_id:ctx.organizationId,p_location_id:locationId,p_scope:String(body.scope||"all"),p_actor_id:ctx.subjectId});if(error)dbFail(error,"start_count",requestId);return data}
export async function getInventoryCount(ctx:InventoryContext,body:any,requestId:string){const countId=asUuid(body.countId,"count");const{data:count,error}=await db.from("inventory_counts").select("*").eq("organization_id",ctx.organizationId).eq("id",countId).single();if(error)dbFail(error,"count",requestId);await requirePermission(ctx,String(count.location_id),"adjust",requestId);const{data:lines,error:le}=await db.from("inventory_count_lines").select("item_id,system_quantity,counted_quantity,variance").eq("organization_id",ctx.organizationId).eq("count_id",countId).order("item_id");if(le)dbFail(le,"count_lines",requestId);const ids=(lines||[]).map((r:any)=>r.item_id);const{data:items,error:ie}=ids.length?await db.from("inventory_items").select("id,name,sku,base_uom").eq("organization_id",ctx.organizationId).in("id",ids):{data:[],error:null};if(ie)dbFail(ie,"count_items",requestId);const im=new Map((items||[]).map((i:any)=>[String(i.id),i]));return{count,lines:(lines||[]).map((l:any)=>({...l,systemQuantity:Number(l.system_quantity),countedQuantity:l.counted_quantity==null?null:Number(l.counted_quantity),variance:l.variance==null?null:Number(l.variance),item:im.get(String(l.item_id))}))}}
export async function setInventoryCountLine(ctx:InventoryContext,body:any,requestId:string){const countId=asUuid(body.countId,"count"),itemId=asUuid(body.itemId,"item"),qty=Number(body.countedQuantity);if(!Number.isFinite(qty)||qty<0)fail(400,"count_quantity_invalid","Gezählte Menge ist ungültig.");const{data:count,error}=await db.from("inventory_counts").select("location_id,status").eq("organization_id",ctx.organizationId).eq("id",countId).single();if(error)dbFail(error,"count_line_count",requestId);await requirePermission(ctx,String(count.location_id),"adjust",requestId);if(count.status!=="counting")fail(409,"count_state_invalid","Zählung kann nicht mehr bearbeitet werden.");const{error:ue}=await db.from("inventory_count_lines").update({counted_quantity:qty,updated_by:ctx.subjectId,updated_at:new Date().toISOString()}).eq("organization_id",ctx.organizationId).eq("count_id",countId).eq("item_id",itemId);if(ue)dbFail(ue,"set_count_line",requestId);return{countId,itemId,countedQuantity:qty}}
export async function postInventoryCount(ctx:InventoryContext,body:any,requestId:string){const countId=asUuid(body.countId,"count");const{data:count,error}=await db.from("inventory_counts").select("location_id,version").eq("organization_id",ctx.organizationId).eq("id",countId).single();if(error)dbFail(error,"post_count_load",requestId);await requirePermission(ctx,String(count.location_id),"adjust",requestId);const{data,error:pe}=await db.rpc("aora_inventory_post_count",{p_organization_id:ctx.organizationId,p_count_id:countId,p_actor_id:ctx.subjectId,p_actor_role:ctx.accessRole,p_expected_version:Number(body.expectedVersion||count.version)});if(pe){if(String(pe.message).includes("incomplete"))fail(409,"count_incomplete","Bitte zuerst alle Artikel zählen.");if(String(pe.message).includes("conflict"))fail(409,"count_conflict","Die Zählung wurde bereits geändert. Bitte aktualisieren.");dbFail(pe,"post_count",requestId)}return data}
export async function receivePurchaseOrderLine(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"receipt",requestId);const{data,error}=await db.rpc("aora_inventory_receive_purchase_order_line",{p_organization_id:ctx.organizationId,p_location_id:locationId,p_purchase_order_id:asUuid(body.purchaseOrderId,"purchase_order"),p_item_id:asUuid(body.itemId,"item"),p_pack_unit_id:asUuid(body.packUnitId,"pack_unit"),p_pack_count:Math.trunc(Number(body.packCount)),p_actor_id:ctx.subjectId,p_actor_role:ctx.accessRole,p_idempotency_key:idem(body.idempotencyKey)});if(error){if(String(error.message).includes("quantity_exceeded"))fail(409,"purchase_order_quantity_exceeded","Die eingegangene Menge überschreitet die offene Bestellmenge.");if(String(error.message).includes("not_receivable"))fail(409,"purchase_order_not_receivable","Diese Bestellung kann nicht mehr angenommen werden.");dbFail(error,"receive_po",requestId)}return data}
export async function issueQrShortCode(ctx:InventoryContext,body:any,requestId:string){const locationId=String(body.locationId||"");await requirePermission(ctx,locationId,"consume",requestId);if(ctx.accessRole==="employee")await requireFeature(ctx,"inventory_employee_scan",locationId,requestId);const code=String(body.shortCode||"").trim();if(code.length<6||code.length>24)fail(400,"short_code_invalid","Kurzcode ist ungültig.");const{data,error}=await db.rpc("aora_inventory_issue_qr_short_code",{p_organization_id:ctx.organizationId,p_location_id:locationId,p_short_code:code,p_actor_id:ctx.subjectId,p_actor_role:ctx.accessRole,p_idempotency_key:idem(body.idempotencyKey)});if(error){const m=String(error.message||"");if(m.includes("already_used"))fail(409,"qr_already_used","Diese Einheit wurde bereits verwendet oder gehört zu einem anderen Standort.");if(m.includes("not_found"))fail(404,"qr_not_found","Kurzcode wurde nicht gefunden.");dbFail(error,"short_code",requestId)}return data}
